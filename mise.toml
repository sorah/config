# `mise bootstrap` and [bootstrap.macos] set this floor.
min_version = "2026.9.11"

# Machine setup for `mise bootstrap` -- https://mise.jdx.dev/bootstrap.html
#
# This file holds the macOS casks that install as plain app bundles, the Arch
# package lists, the macOS defaults and the dotfile symlinks. The Brewfile holds
# the casks whose installers need sudo, the formulae and the Mac App Store apps.
# setup.sh holds the rest: the aur-sorah repo, rustup, gopls, claude plugins and
# the systemd units.
#
#   mise bootstrap status
#   mise bootstrap packages status
#   mise bootstrap packages apply --dry-run
#   mise bootstrap packages apply
#
# Homebrew is not required. mise pours into /opt/homebrew itself and counts a
# brew-installed cask as installed, leaving the app bundle and its TCC grants.

[bootstrap.packages]
"brew-cask:1password" = { os = "macos" }
"brew-cask:google-chrome@dev" = { os = "macos" }
"brew-cask:firefox@developer-edition" = { os = "macos" }
"brew-cask:aquaskk" = { os = "macos" }
"brew-cask:logseq" = { os = "macos" }
"brew-cask:linearmouse" = { os = "macos" }
"brew-cask:the-unarchiver" = { os = "macos" }
"brew-cask:wezterm" = { os = "macos" }
"brew-cask:visual-studio-code@insiders" = { os = "macos" }
"brew-cask:discord@ptb" = { os = "macos" }
"brew-cask:slack" = { os = "macos" }
"brew-cask:spotify" = { os = "macos" }
"brew-cask:vlc" = { os = "macos" }
"brew-cask:figma" = { os = "macos" }

# Arch. mise skips a manager the machine does not have, so no os key is needed.
# Arch repositories carry one version of each package, so only "latest" works.

# base system, build toolchain
"pacman:base-devel" = "latest"
"pacman:cmake" = "latest"
"pacman:mold" = "latest"
"pacman:file" = "latest"
"pacman:findutils" = "latest"
"pacman:grep" = "latest"
"pacman:curl" = "latest"
"pacman:zip" = "latest"
"pacman:pv" = "latest"
"pacman:parallel" = "latest"

# vcs
"pacman:git" = "latest"
"pacman:mercurial" = "latest"
"pacman:subversion" = "latest"

# shell, terminal, editor
"pacman:zsh" = "latest"
"pacman:tmux" = "latest"
"pacman:screen" = "latest"
"pacman:neovim" = "latest"

# crypto and secrets
"pacman:gnupg" = "latest"
"pacman:pinentry" = "latest"
"pacman:keychain" = "latest"
"pacman:openssl" = "latest"
"pacman:cfssl" = "latest"
"pacman:cosign" = "latest"
"pacman:patatt" = "latest"

# networking. netcat, bind-tools and ebtables are virtual provides.
"pacman:whois" = "latest"
"pacman:ipcalc" = "latest"
"pacman:iperf" = "latest"
"pacman:mtr" = "latest"
"pacman:nmap" = "latest"
"pacman:netcat" = "latest"
"pacman:tcpdump" = "latest"
"pacman:traceroute" = "latest"
"pacman:bind-tools" = "latest"
"pacman:wireguard-tools" = "latest"
"pacman:ethtool" = "latest"
"pacman:ldns" = "latest"
"pacman:inetutils" = "latest"
"pacman:ebtables" = "latest"
"pacman:nftables" = "latest"
"pacman:swaks" = "latest"

# disks, hardware, process inspection
"pacman:smartmontools" = "latest"
"pacman:usbutils" = "latest"
"pacman:cryptsetup" = "latest"
"pacman:btrfs-progs" = "latest"
"pacman:dosfstools" = "latest"
"pacman:lvm2" = "latest"
"pacman:xfsprogs" = "latest"
"pacman:e2fsprogs" = "latest"
"pacman:dool" = "latest"
"pacman:htop" = "latest"
"pacman:iotop" = "latest"
"pacman:lsof" = "latest"
"pacman:strace" = "latest"

# languages. The macOS equivalents are in mise/tools.toml.
"pacman:go" = "latest"
"pacman:go-tools" = "latest"
"pacman:ruby" = "latest"
"pacman:ruby-irb" = "latest"
"pacman:ruby-erb" = "latest"
"pacman:rbenv" = "latest"
"pacman:nodejs" = "latest"
"pacman:python-pip" = "latest"
"pacman:python-pipx" = "latest"

# cli tools that mise/tools.toml installs itself on macOS
"pacman:jq" = "latest"
"pacman:fzf" = "latest"
"pacman:ripgrep" = "latest"
"pacman:ghq" = "latest"
"pacman:github-cli" = "latest"
"pacman:protobuf" = "latest"
# An installed bazelisk-bin satisfies this, providing bazelisk.
"pacman:bazelisk" = "latest"

# database clients
"pacman:postgresql-libs" = "latest"
"pacman:mariadb-clients" = "latest"

# containers, cloud. amazon-ecr-credential-helper comes from aur-sorah, a
# pacman repo, not the AUR.
"pacman:docker-buildx" = "latest"
"pacman:amazon-ecr-credential-helper" = "latest"

# misc
"pacman:imagemagick" = "latest"
"pacman:man-db" = "latest"
"pacman:man-pages" = "latest"
"pacman:texinfo" = "latest"

# AUR, built by the yay in mise/tools.toml. setup.sh applies these after the
# tools phase that installs it.
#
# mise omits --needed for these, so an aur: entry replaces a repository package
# of the same name.
"aur:cloudflared-bin" = "latest"
"aur:aws-session-manager-plugin" = "latest"
"aur:terraform-ls" = "latest"
"aur:envchain" = "latest"
"aur:overmind" = "latest"
"aur:tio" = "latest"
"aur:bridge-utils" = "latest"
# debian packaging
"aur:debianutils" = "latest"
"aur:devscripts" = "latest"
"aur:git-buildpackage" = "latest"
"aur:pristine-tar" = "latest"
"aur:perl-file-rename" = "latest"

# ----- dotfiles ---------------------------------------------------------------
#
# Sources are relative to this file. Parent directories are created as needed.
#
#   mise bootstrap dotfiles status
#   mise bootstrap dotfiles apply --dry-run
#
# mise refuses to overwrite a target it did not create, and one conflict aborts
# the whole phase. setup.sh clears conflicting targets before applying.
#
# setup.sh owns ~/.tmux.reattacher, ghq.root and include.path. The git settings
# stay `git config --global` so a machine can override them.

[dotfiles]
# vim / neovim. dot.vim and dot.vimrc each serve two targets.
"~/.vim"                           = "vim/dot.vim"
"~/.local/share/nvim/site"         = "vim/dot.vim"
"~/.vimrc"                         = "vim/dot.vimrc"
"~/.config/nvim/init.vim"          = "vim/dot.vimrc"
"~/.config/nvim/coc-settings.json" = "vim/coc-settings.json"

# shell / terminal
"~/.zshrc"                      = "zsh/dot.zshrc"
"~/.tmux.conf"                  = "tmux/tmux.conf"
"~/.config/wezterm/wezterm.lua" = "wezterm.lua"

# ruby
"~/.irbrc" = "misc/dot.irbrc"
"~/.gemrc" = "misc/dot.gemrc"

# Karabiner and LinearMouse rewrite their config in place, replacing a symlink
# with a regular file, so these are copies with the repo as source of truth. An
# app-side edit lives only in the target until captured:
#
#   mise bootstrap dotfiles add --changed
"~/.config/karabiner/karabiner.json"     = { source = "mac/dot.config/karabiner/karabiner.json", mode = "copy" }
"~/.config/linearmouse/linearmouse.json" = { source = "mac/dot.config/linearmouse/linearmouse.json", mode = "copy" }

# ~/.config/mise/conf.d/*.toml is read in every directory, whereas [tools] in
# this file would only apply under ~/git/config, so the global tool set is
# deployed there as a fragment. Under `paranoid` the deployed path needs
# trusting once:
#
#   mise trust ~/.config/mise/conf.d/sorah-tools.toml
"~/.config/mise/conf.d/sorah-tools.toml" = { source = "mise/tools.toml", mode = "symlink" }

# A dotfile entry has no `os` key of its own. The key becomes a logical name,
# each variant supplies a target, and an unmatched entry is not deployed.
#
# zsh/gentoo.zshrc_global_env is not declared. It hardcodes /usr/lib64/qt5, so
# os = "linux" would be wrong on Arch, and ~/.zshrc sources it only if present.
[dotfiles.zshrc_global_env]
source = "zsh/mac.zshrc_global_env"
variants = [{ os = "macos", target = "~/.zshrc_global_env" }]

[dotfiles.sorah_browser_desktop]
source = "linux/dot.local/share/applications/sorah-browser.desktop"
variants = [{ os = "linux", target = "~/.local/share/applications/sorah-browser.desktop" }]

# ----- macOS preferences ------------------------------------------------------
#
# Captured from this Mac on macOS 27.0 (26A5406e) in 2026-08, holding only the
# settings that differ from stock. Applying therefore converges a fresh Mac
# rather than undoing drift on a used one, and `macos defaults status` is the
# drift check for the keys declared here.
#
# Stock values live in each app's compiled-in registerDefaults, not in any
# plist, so the trim rests on judgement. Writing one that turns out to be stock
# is harmless.
#
# MDM overrides some of this on managed machines.
#
#   mise bootstrap macos defaults status
#   mise bootstrap macos defaults apply --dry-run
#   mise bootstrap macos defaults apply
#
# A logout/login is needed for input sources (AquaSKK/ABC), the fn key action,
# and modifier-key remapping on already-connected keyboards.
#
# Types are load-bearing. mise counts a preference as in sync only when the
# plist type matches, so keys macOS stores as reals are floats here (25.0, not
# 25). Raw domains throughout, rather than the [bootstrap.macos.dock] and
# [macos.keyboard] shorthands, which would write integers for some of these.

[bootstrap.macos.defaults.NSGlobalDomain]
# Locale / language
AppleLanguages = ["en-GB", "ja-GB"]
AppleLocale = "en_GB"
# Stock for en_GB, but not for a US-provisioned machine, so set explicitly
AppleMeasurementUnits = "Centimeters"
AppleMetricUnits = true

# Fast key repeat: 375ms delay, 30ms interval (stock is 68 / 6)
InitialKeyRepeat = 25.0
KeyRepeat = 2.0
# F1/F2 etc. behave as standard function keys
"com.apple.keyboard.fnState" = true
# Full Keyboard Access: Tab moves between all controls, not just text boxes
AppleKeyboardUIMode = 3

# Trackpad speeds (tracking, scroll) and spring-loaded folder delay. Every
# gesture on this machine is at its factory setting, so only the speeds are
# declared.
"com.apple.trackpad.scaling" = 2.0
"com.apple.trackpad.scrolling" = 0.1838
"com.apple.springing.delay" = 0.7575731128454208
# No two-finger swipe-to-navigate-back in apps
AppleEnableSwipeNavigateWithScrolls = false

# Windows / Finder
AppleShowScrollBars = "Always"
AppleShowAllExtensions = true
# Open/Save dialogs default to list view
NSNavPanelFileListModeForOpenMode2 = 2
NSNavPanelFileListModeForSaveMode2 = 2
NSNavPanelFileLastListModeForOpenModeKey = 2
NSNavPanelFileLastListModeForSaveModeKey = 2

# Misc
"com.apple.sound.beep.sound" = "/System/Library/Sounds/Sosumi.aiff"
shouldShowRSVPDataDetectors = false

[bootstrap.macos.defaults."com.apple.HIToolbox"]
# fn key action, copied verbatim. Apple documents no mapping for the value, so
# which UI option 0 means is unconfirmed.  [logout]
AppleFnUsageType = 0
# Input sources: ABC layout + AquaSKK (Japanese, declared in com.apple.inputsources
# below). AquaSKK must be installed first, and HIToolbox is only re-read at
# login.  [logout]
AppleEnabledInputSources = [
  { InputSourceKind = "Keyboard Layout", "KeyboardLayout ID" = 252, "KeyboardLayout Name" = "ABC" },
  { "Bundle ID" = "com.apple.CharacterPaletteIM", InputSourceKind = "Non Keyboard Input Method" },
  { "Bundle ID" = "com.apple.PressAndHold", InputSourceKind = "Non Keyboard Input Method" },
]

[bootstrap.macos.defaults."com.apple.inputsources"]
AppleEnabledThirdPartyInputSources = [
  { "Bundle ID" = "jp.sourceforge.inputmethod.aquaskk", InputSourceKind = "Keyboard Input Method" },
  { "Bundle ID" = "jp.sourceforge.inputmethod.aquaskk", "Input Mode" = "com.apple.inputmethod.Japanese", InputSourceKind = "Input Mode" },
]

[bootstrap.macos.defaults."com.apple.keyboard.preferences"]
IsMixmojiSuggestionsEnabled = false

[bootstrap.macos.defaults."com.apple.universalaccess"]
closeViewHotkeysEnabled = false
"com.apple.custommenu.apps" = ["NSGlobalDomain"]

# Magic Mouse: single button (no right-click), no one-finger smart zoom.
#
# com.apple.swipescrolldirection and com.apple.mouse.scaling are left unset.
# LinearMouse (mac/dot.config/linearmouse/linearmouse.json) governs per-mouse
# scroll direction and pointer speed, and writing them here would fight it.
[bootstrap.macos.defaults."com.apple.AppleMultitouchMouse"]
MouseButtonMode = "OneButton"
MouseOneFingerDoubleTapGesture = 0
UserPreferences = true   # marks the set as user-customised

[bootstrap.macos.defaults."com.apple.driver.AppleBluetoothMultitouch.mouse"]
MouseButtonMode = "OneButton"
MouseOneFingerDoubleTapGesture = 0
UserPreferences = true

[bootstrap.macos.defaults."com.apple.dock"]
autohide = true
"autohide-time-modifier" = 0.5
tilesize = 39.0
mineffect = "scale"
"mru-spaces" = false          # don't reorder Spaces by use
"expose-group-apps" = false   # Mission Control: don't group by app
"workspaces-auto-swoosh" = true

[bootstrap.macos.defaults."com.apple.WindowManager"]
EnableTiledWindowMargins = false
HideDesktop = true
AppWindowGroupingBehavior = 1

[bootstrap.macos.defaults."com.apple.finder"]
ShowPathbar = true
ShowStatusBar = true
FXPreferredViewStyle = "Nlsv"   # list view

[bootstrap.macos.defaults."com.apple.menuextra.clock"]
ShowAMPM = true   # 12-hour, vs 24h for en_GB

# Menu bar: hide the Spotlight item (per-host, like `defaults -currentHost`)
[[bootstrap.macos.defaults_entries]]
domain = "com.apple.Spotlight"
key = "MenuItemHidden"
host = "current"
value = true

# Caps Lock -> Control (0x700000039 -> 0x7000000E4) on every keyboard except
# vendor 1452 / product 591, the Karabiner DriverKit VirtualHIDKeyboard, whose
# output would otherwise be remapped twice.
[[bootstrap.macos.defaults_entries]]
domain = "NSGlobalDomain"
key = "com.apple.keyboard.modifiermapping.0-0-0"
host = "current"
value = [{ HIDKeyboardModifierMappingSrc = 30064771129, HIDKeyboardModifierMappingDst = 30064771300 }]

[[bootstrap.macos.defaults_entries]]
domain = "NSGlobalDomain"
key = "com.apple.keyboard.modifiermapping.1452-591-0"
host = "current"
value = [{ HIDKeyboardModifierMappingSrc = 30064771129, HIDKeyboardModifierMappingDst = 30064771129 }]

# Accessibility zoom, invert-colours and contrast hotkeys (IDs 15-26), all off
# so Cmd-Opt-8 and Cmd-Opt-+/- stay available to apps. Each id is patched
# individually. Writing AppleSymbolicHotKeys whole would drop every other
# shortcut.

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["15"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["16"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["17"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["18"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["19"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["20"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["21"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["22"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["23"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["24"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["25"]
value = { enabled = false }

[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["26"]
value = { enabled = false }

# Unidentified system hotkey, disabled. 65535/65535/0 is how macOS records
# a shortcut with no key assigned, so it is kept verbatim.
[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["164"]
value = { enabled = false, value = { parameters = [65535, 65535, 0], type = "standard" } }

# System "Show Emoji & Symbols" (Ctrl-Cmd-Space) off, replaced by the
# Cmd-Shift-. menu equivalent below, which frees Ctrl-Cmd-Space.
[[bootstrap.macos.defaults_entries]]
domain = "com.apple.symbolichotkeys"
key = "AppleSymbolicHotKeys"
path = ["176"]
value = { enabled = false, value = { type = "SAE1.0" } }

[[bootstrap.macos.defaults_entries]]
domain = "NSGlobalDomain"
key = "NSUserKeyEquivalents"
path = ["Emoji & Symbols"]
value = "@$."

# Some apps read changed preferences only after a relaunch, and mise never
# restarts them. This runs on every `mise bootstrap`, changed or not.
[bootstrap.hooks.post-defaults]
run = [
  "killall Dock Finder SystemUIServer ControlCenter 2>/dev/null || true",
  "/System/Library/PrivateFrameworks/SystemAdministration.framework/Resources/activateSettings -u 2>/dev/null || true",
]
